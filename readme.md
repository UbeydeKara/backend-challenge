![backend-challenge](https://ubeydekara.github.io/challenge.drawio.svg)

### Usage

Build the app:
```shell
$ mvn clean package -Pbuild-image
```

Run all the containers with `docker-compose`:
```shell
$ docker-compose up
```